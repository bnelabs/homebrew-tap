# bnelabs Homebrew Tap

Install r105 with:

```sh
brew install bnelabs/tap/r105
```

The formula installs the Python package and its pinned runtime dependencies
inside an isolated Homebrew virtual environment.
